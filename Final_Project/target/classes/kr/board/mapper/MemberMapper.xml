<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.board.mapper.MemberMapper">
	<!-- 회원가입 -->
	<insert id="sign" parameterType="kr.board.entity.TMember">
		insert into Member(memId, memPw, memNick, memType, memGender,memEmail)
		values(#{mem_id},#{mem_pw},#{mem_nick},#{mem_type},#(mem_gender),#(mem_email))
	</insert>

	<!-- 로그인 -->
	<select id="Login" parameterType="kr.board.entity.TMember"
		resultType="kr.board.entity.TMember">
		select * from Mem ber where memId=#{mem_id} and memPw=#{mem_pw}
	</select>
</mapper>